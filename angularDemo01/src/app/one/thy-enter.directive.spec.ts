import { ThyEnterDirective } from './thy-enter.directive';

describe('ThyEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new ThyEnterDirective();
    expect(directive).toBeTruthy();
  });
});
