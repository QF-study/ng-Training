export type Banner = {
  targetId?: number;
  url?: string;
  imageUrl?: string;
};

export type HotTag = {
  name: string;
  id: number;
  position: number;
}

export type SongSheet = {
  name: string;
  id: number;
  picUrl: string;
  playCount: number;
}